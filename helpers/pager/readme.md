A pager should allow for the navigation of paginated data.

It should track the number of items, number of items per page, and current page.

It should display a pager control, summary, and a page size control.